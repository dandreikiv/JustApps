import SwiftUI

// MARK: - Views
struct ContentView: View {
    @State var viewModel = MarketsViewModel()

    var body: some View {
        NavigationView {

        }
    }
}

#Preview {
    ContentView()
}
